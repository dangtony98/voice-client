import React, { useState } from 'react';
import { View, Text, StyleSheet } from 'react-native';

export default () => {
  return (
    <View>
      <Text>Username Step</Text>
    </View>
  );
}

const styles = StyleSheet.create({

});